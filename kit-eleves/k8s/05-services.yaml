apiVersion: v1
kind: Service
metadata:
  name: taskmanager
  namespace: taskmanager
spec:
  type: NodePort
  selector:
    app: taskmanager
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080