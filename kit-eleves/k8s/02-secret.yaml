apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: # ???
type: Opaque
stringData:
  # ??? définissez ici la clé contenant le mot de passe Redis
  # Choisissez un nom de clé cohérent — vous l'utiliserez dans 03-redis.yaml et 04-app.yaml
  REDIS_PASSWORD: "???"    # remplacez par un vrai mot de passe

---
# ??? Si l'application nécessite d'autres secrets (ex: credentials de registre privé),
# ajoutez-les ici ou dans un fichier séparé.
